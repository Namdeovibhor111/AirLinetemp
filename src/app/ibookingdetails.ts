export interface Ibookingdetails {
    bId:Number,
    cId: Number,
    fId: Number,
    bookingDate: string,
    flightDate: string,
    passenger: Number,
    totalFare: Number,
    bookingStatus: string
}
